---
title: "Rust API Reference"
---

<!-- This file is auto-generated by alef — DO NOT EDIT. -->
<!-- alef:hash:c76d1d7a3efd8586b2904b8df017457c5ee1fb309a85a56f68afaa9a10d485bf -->
<!-- To regenerate: alef docs -->
<!-- To verify freshness: alef verify -->

## Rust API Reference <span class="version-badge">v0.2.0</span>

### Functions

#### unregister_workflow_host()

**Signature:**

```rust
pub fn unregister_workflow_host(name: &str) -> Result<(), HostPrototypeError>
```

**Example:**

```rust
unregister_workflow_host("value")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `name` | `&str` | Yes | The name |

**Returns:** No return value.

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### clear_workflow_hosts()

**Signature:**

```rust
pub fn clear_workflow_hosts() -> Result<(), HostPrototypeError>
```

**Example:**

```rust
clear_workflow_hosts()?;
```

**Returns:** No return value.

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### unregister_parser_host()

**Signature:**

```rust
pub fn unregister_parser_host(name: &str) -> Result<(), HostPrototypeError>
```

**Example:**

```rust
unregister_parser_host("value")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `name` | `&str` | Yes | The name |

**Returns:** No return value.

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### clear_parser_hosts()

**Signature:**

```rust
pub fn clear_parser_hosts() -> Result<(), HostPrototypeError>
```

**Example:**

```rust
clear_parser_hosts()?;
```

**Returns:** No return value.

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### replace_workflow_host()

**Signature:**

```rust
pub fn replace_workflow_host(provider: WorkflowHost, name: String) -> Result<(), HostPrototypeError>
```

**Example:**

```rust
replace_workflow_host(WorkflowHost::default(), "value")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider` | `WorkflowHost` | Yes | The workflow host |
| `name` | `String` | Yes | The name |

**Returns:** No return value.

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### register_tslp_parser_host()

**Signature:**

```rust
pub fn register_tslp_parser_host(provider_name: String, language: String) -> Result<(), HostPrototypeError>
```

**Example:**

```rust
register_tslp_parser_host("value", "value")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `language` | `String` | Yes | The language |

**Returns:** No return value.

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### replace_parser_host()

**Signature:**

```rust
pub fn replace_parser_host(provider: ParserHost, name: String) -> Result<(), HostPrototypeError>
```

**Example:**

```rust
replace_parser_host(ParserHost::default(), "value")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider` | `ParserHost` | Yes | The parser host |
| `name` | `String` | Yes | The name |

**Returns:** No return value.

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### execute_identity()

**Signature:**

```rust
pub fn execute_identity(provider_name: String, request: Vec<u8>) -> Result<Vec<u8>, HostPrototypeError>
```

**Example:**

```rust
let result = execute_identity("value", b"data")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `request` | `Vec<u8>` | Yes | The request |

**Returns:** `Vec<u8>`

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### execute_async_identity()

**Signature:**

```rust
pub async fn execute_async_identity(provider_name: String, request: Vec<u8>) -> Result<Vec<u8>, HostPrototypeError>
```

**Example:**

```rust
let result = execute_async_identity("value", b"data").await?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `request` | `Vec<u8>` | Yes | The request |

**Returns:** `Vec<u8>`

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### start_identity_worker()

**Signature:**

```rust
pub fn start_identity_worker(provider_name: String, request: Vec<u8>) -> Result<u64, HostPrototypeError>
```

**Example:**

```rust
let result = start_identity_worker("value", b"data")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `request` | `Vec<u8>` | Yes | The request |

**Returns:** `u64`

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### prepare_identity_worker()

**Signature:**

```rust
pub fn prepare_identity_worker(provider_name: String, request: Vec<u8>) -> Result<u64, HostPrototypeError>
```

**Example:**

```rust
let result = prepare_identity_worker("value", b"data")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `request` | `Vec<u8>` | Yes | The request |

**Returns:** `u64`

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### dispatch_identity_worker()

**Signature:**

```rust
pub fn dispatch_identity_worker(task_id: u64) -> Result<(), HostPrototypeError>
```

**Example:**

```rust
dispatch_identity_worker(42)?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `task_id` | `u64` | Yes | The task id |

**Returns:** No return value.

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### cancel_identity_worker()

**Signature:**

```rust
pub fn cancel_identity_worker(task_id: u64) -> Result<(), HostPrototypeError>
```

**Example:**

```rust
cancel_identity_worker(42)?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `task_id` | `u64` | Yes | The task id |

**Returns:** No return value.

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### identity_worker_cancelled()

**Signature:**

```rust
pub fn identity_worker_cancelled(task_id: u64) -> Result<bool, HostPrototypeError>
```

**Example:**

```rust
let result = identity_worker_cancelled(42)?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `task_id` | `u64` | Yes | The task id |

**Returns:** `bool`

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### poll_identity_worker()

**Signature:**

```rust
pub fn poll_identity_worker(task_id: u64) -> Result<Option<Vec<u8>>, HostPrototypeError>
```

**Example:**

```rust
let result = poll_identity_worker(42)?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `task_id` | `u64` | Yes | The task id |

**Returns:** `Option<Vec<u8>>`

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### start_host_runtime()

**Signature:**

```rust
pub fn start_host_runtime() -> Result<(), HostPrototypeError>
```

**Example:**

```rust
start_host_runtime()?;
```

**Returns:** No return value.

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### shutdown_host_runtime()

**Signature:**

```rust
pub async fn shutdown_host_runtime(timeout_millis: u64, force: bool) -> Result<Vec<u64>, HostPrototypeError>
```

**Example:**

```rust
let result = shutdown_host_runtime(42, true).await?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `timeout_millis` | `u64` | Yes | The timeout millis |
| `force` | `bool` | Yes | The force |

**Returns:** `Vec<u64>`

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### execute_typed_identity()

**Signature:**

```rust
pub fn execute_typed_identity(provider_name: String, source_ids: Vec<String>, source_lengths: Vec<u64>, source_digests: Vec<String>, source: Vec<u8>) -> Result<Vec<u8>, HostPrototypeError>
```

**Example:**

```rust
let result = execute_typed_identity("value", vec![], vec![], vec![], b"data")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `source_ids` | `Vec<String>` | Yes | The source ids |
| `source_lengths` | `Vec<u64>` | Yes | The source lengths |
| `source_digests` | `Vec<String>` | Yes | The source digests |
| `source` | `Vec<u8>` | Yes | The source |

**Returns:** `Vec<u8>`

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### execute_typed_workflow()

**Signature:**

```rust
pub fn execute_typed_workflow(provider_name: String, source_ids: Vec<String>, source_lengths: Vec<u64>, source_digests: Vec<String>, source: Vec<u8>) -> Result<Vec<u8>, HostPrototypeError>
```

**Example:**

```rust
let result = execute_typed_workflow("value", vec![], vec![], vec![], b"data")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `source_ids` | `Vec<String>` | Yes | The source ids |
| `source_lengths` | `Vec<u64>` | Yes | The source lengths |
| `source_digests` | `Vec<String>` | Yes | The source digests |
| `source` | `Vec<u8>` | Yes | The source |

**Returns:** `Vec<u8>`

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### execute_in_process_identity()

**Signature:**

```rust
pub fn execute_in_process_identity(source_ids: Vec<String>, source_lengths: Vec<u64>, source_digests: Vec<String>, source: Vec<u8>) -> Result<Vec<u8>, HostPrototypeError>
```

**Example:**

```rust
let result = execute_in_process_identity(vec![], vec![], vec![], b"data")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `source_ids` | `Vec<String>` | Yes | The source ids |
| `source_lengths` | `Vec<u64>` | Yes | The source lengths |
| `source_digests` | `Vec<String>` | Yes | The source digests |
| `source` | `Vec<u8>` | Yes | The source |

**Returns:** `Vec<u8>`

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### execute_detached_identity()

**Signature:**

```rust
pub fn execute_detached_identity(provider_name: String, source_ids: Vec<String>, source_lengths: Vec<u64>, source_digests: Vec<String>, source: Vec<u8>) -> Result<Vec<u8>, HostPrototypeError>
```

**Example:**

```rust
let result = execute_detached_identity("value", vec![], vec![], vec![], b"data")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `source_ids` | `Vec<String>` | Yes | The source ids |
| `source_lengths` | `Vec<u64>` | Yes | The source lengths |
| `source_digests` | `Vec<String>` | Yes | The source digests |
| `source` | `Vec<u8>` | Yes | The source |

**Returns:** `Vec<u8>`

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### registered_workflow_hosts()

**Signature:**

```rust
pub fn registered_workflow_hosts() -> Vec<String>
```

**Example:**

```rust
let result = registered_workflow_hosts();
```

**Returns:** `Vec<String>`

---

#### probe_with_parser()

**Signature:**

```rust
pub fn probe_with_parser(provider_name: String, request: Vec<u8>) -> Result<Vec<u8>, HostPrototypeError>
```

**Example:**

```rust
let result = probe_with_parser("value", b"data")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `request` | `Vec<u8>` | Yes | The request |

**Returns:** `Vec<u8>`

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### parse_with_parser()

**Signature:**

```rust
pub fn parse_with_parser(provider_name: String, request: Vec<u8>) -> Result<Vec<u8>, HostPrototypeError>
```

**Example:**

```rust
let result = parse_with_parser("value", b"data")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `request` | `Vec<u8>` | Yes | The request |

**Returns:** `Vec<u8>`

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### registered_parser_hosts()

**Signature:**

```rust
pub fn registered_parser_hosts() -> Vec<String>
```

**Example:**

```rust
let result = registered_parser_hosts();
```

**Returns:** `Vec<String>`

---

#### capability_manifest()

**Signature:**

```rust
pub fn capability_manifest() -> Result<String, HostPrototypeError>
```

**Example:**

```rust
let result = capability_manifest()?;
```

**Returns:** `String`

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### merge_json_two_way()

**Signature:**

```rust
pub fn merge_json_two_way(incoming_source: String, current_source: String, dialect: String) -> Result<String, HostPrototypeError>
```

**Example:**

```rust
let result = merge_json_two_way("value", "value", "value")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `incoming_source` | `String` | Yes | The incoming source |
| `current_source` | `String` | Yes | The current source |
| `dialect` | `String` | Yes | The dialect |

**Returns:** `String`

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### merge_json_three_way()

**Signature:**

```rust
pub fn merge_json_three_way(base_source: String, ours_source: String, theirs_source: String, dialect: String) -> Result<String, HostPrototypeError>
```

**Example:**

```rust
let result = merge_json_three_way("value", "value", "value", "value")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `base_source` | `String` | Yes | The base source |
| `ours_source` | `String` | Yes | The ours source |
| `theirs_source` | `String` | Yes | The theirs source |
| `dialect` | `String` | Yes | The dialect |

**Returns:** `String`

**Errors:** Returns `Err(HostPrototypeError)`.

---

### Types

#### HostBatchRequest

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `schema` | `String` | — | Schema |
| `items` | `Vec<HostSourceSegment>` | — | Items |

---

#### HostPrototypeError

##### Methods

###### new()

**Signature:**

```rust
pub fn new(message: String) -> HostPrototypeError
```

**Example:**

```rust
let result = HostPrototypeError::new("value");
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `message` | `String` | Yes | The message |

**Returns:** `HostPrototypeError`

###### message()

**Signature:**

```rust
pub fn message(&self) -> String
```

**Example:**

```rust
let result = instance.message();
```

**Returns:** `String`

---

#### HostSourceSegment

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `source_id` | `String` | — | Source id |
| `offset` | `u64` | — | Offset |
| `byte_length` | `u64` | — | Byte length |
| `sha256` | `String` | — | Sha256 |

---

#### ParserHost

##### Methods

###### descriptor()

**Signature:**

```rust
pub fn descriptor(&self) -> Result<String, HostPrototypeError>
```

**Example:**

```rust
let result = instance.descriptor()?;
```

**Returns:** `String`

**Errors:** Returns `Err(HostPrototypeError)`.

###### probe_batch()

**Signature:**

```rust
pub fn probe_batch(&self, request: Vec<u8>) -> Result<Vec<u8>, HostPrototypeError>
```

**Example:**

```rust
let result = instance.probe_batch(b"data")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `request` | `Vec<u8>` | Yes | The request |

**Returns:** `Vec<u8>`

**Errors:** Returns `Err(HostPrototypeError)`.

###### parse_batch()

**Signature:**

```rust
pub fn parse_batch(&self, request: Vec<u8>) -> Result<Vec<u8>, HostPrototypeError>
```

**Example:**

```rust
let result = instance.parse_batch(b"data")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `request` | `Vec<u8>` | Yes | The request |

**Returns:** `Vec<u8>`

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### Plugin

##### Methods

###### name()

**Signature:**

```rust
pub fn name(&self) -> String
```

**Example:**

```rust
let result = instance.name();
```

**Returns:** `String`

###### version()

**Signature:**

```rust
pub fn version(&self) -> Result<String, HostPrototypeError>
```

**Example:**

```rust
let result = instance.version()?;
```

**Returns:** `String`

**Errors:** Returns `Err(HostPrototypeError)`.

###### initialize()

**Signature:**

```rust
pub fn initialize(&self) -> Result<(), HostPrototypeError>
```

**Example:**

```rust
instance.initialize()?;
```

**Returns:** No return value.

**Errors:** Returns `Err(HostPrototypeError)`.

###### shutdown()

**Signature:**

```rust
pub fn shutdown(&self) -> Result<(), HostPrototypeError>
```

**Example:**

```rust
instance.shutdown()?;
```

**Returns:** No return value.

**Errors:** Returns `Err(HostPrototypeError)`.

---

#### WorkflowHost

##### Methods

###### descriptor()

**Signature:**

```rust
pub fn descriptor(&self) -> Result<String, HostPrototypeError>
```

**Example:**

```rust
let result = instance.descriptor()?;
```

**Returns:** `String`

**Errors:** Returns `Err(HostPrototypeError)`.

###### execute_batch()

**Signature:**

```rust
pub fn execute_batch(&self, request: Vec<u8>) -> Result<Vec<u8>, HostPrototypeError>
```

**Example:**

```rust
let result = instance.execute_batch(b"data")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `request` | `Vec<u8>` | Yes | The request |

**Returns:** `Vec<u8>`

**Errors:** Returns `Err(HostPrototypeError)`.

###### execute_cancellable_batch()

**Signature:**

```rust
pub fn execute_cancellable_batch(&self, task_id: u64, request: Vec<u8>) -> Result<Vec<u8>, HostPrototypeError>
```

**Example:**

```rust
let result = instance.execute_cancellable_batch(42, b"data")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `task_id` | `u64` | Yes | The task id |
| `request` | `Vec<u8>` | Yes | The request |

**Returns:** `Vec<u8>`

**Errors:** Returns `Err(HostPrototypeError)`.

###### execute_async_batch()

**Signature:**

```rust
pub async fn execute_async_batch(&self, request: Vec<u8>) -> Result<Vec<u8>, HostPrototypeError>
```

**Example:**

```rust
let result = instance.execute_async_batch(b"data").await?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `request` | `Vec<u8>` | Yes | The request |

**Returns:** `Vec<u8>`

**Errors:** Returns `Err(HostPrototypeError)`.

###### execute_typed_batch()

**Signature:**

```rust
pub fn execute_typed_batch(&self, request: HostBatchRequest, source: Vec<u8>) -> Result<Vec<u8>, HostPrototypeError>
```

**Example:**

```rust
let result = instance.execute_typed_batch(HostBatchRequest::default(), b"data")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `request` | `HostBatchRequest` | Yes | The host batch request |
| `source` | `Vec<u8>` | Yes | The source |

**Returns:** `Vec<u8>`

**Errors:** Returns `Err(HostPrototypeError)`.

###### execute_detached_batch()

**Signature:**

```rust
pub fn execute_detached_batch(&self, request: Vec<u8>) -> Result<Vec<u8>, HostPrototypeError>
```

**Example:**

```rust
let result = instance.execute_detached_batch(b"data")?;
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `request` | `Vec<u8>` | Yes | The request |

**Returns:** `Vec<u8>`

**Errors:** Returns `Err(HostPrototypeError)`.

---
