---
title: "Ruby API Reference"
---

<!-- This file is auto-generated by alef — DO NOT EDIT. -->
<!-- alef:hash:7a09ddfa4724b5bed9fe47ddd02a34c5c5bccec69a00c11693549e0861c80f79 -->
<!-- To regenerate: alef docs -->
<!-- To verify freshness: alef verify -->

## Ruby API Reference <span class="version-badge">v0.2.0</span>

### Functions

#### unregister_workflow_host()

**Signature:**

```ruby
def self.unregister_workflow_host(name)
```

**Example:**

```ruby
unregister_workflow_host("value")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `name` | `String` | Yes | The name |

**Returns:** No return value.

**Errors:** Raises `HostPrototypeError`.

---

#### clear_workflow_hosts()

**Signature:**

```ruby
def self.clear_workflow_hosts()
```

**Example:**

```ruby
clear_workflow_hosts()
```

**Returns:** No return value.

**Errors:** Raises `HostPrototypeError`.

---

#### unregister_parser_host()

**Signature:**

```ruby
def self.unregister_parser_host(name)
```

**Example:**

```ruby
unregister_parser_host("value")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `name` | `String` | Yes | The name |

**Returns:** No return value.

**Errors:** Raises `HostPrototypeError`.

---

#### clear_parser_hosts()

**Signature:**

```ruby
def self.clear_parser_hosts()
```

**Example:**

```ruby
clear_parser_hosts()
```

**Returns:** No return value.

**Errors:** Raises `HostPrototypeError`.

---

#### replace_workflow_host()

**Signature:**

```ruby
def self.replace_workflow_host(provider, name)
```

**Example:**

```ruby
replace_workflow_host(WorkflowHost.new, "value")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider` | `WorkflowHost` | Yes | The workflow host |
| `name` | `String` | Yes | The name |

**Returns:** No return value.

**Errors:** Raises `HostPrototypeError`.

---

#### register_tslp_parser_host()

**Signature:**

```ruby
def self.register_tslp_parser_host(provider_name, language)
```

**Example:**

```ruby
register_tslp_parser_host("value", "value")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `language` | `String` | Yes | The language |

**Returns:** No return value.

**Errors:** Raises `HostPrototypeError`.

---

#### replace_parser_host()

**Signature:**

```ruby
def self.replace_parser_host(provider, name)
```

**Example:**

```ruby
replace_parser_host(ParserHost.new, "value")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider` | `ParserHost` | Yes | The parser host |
| `name` | `String` | Yes | The name |

**Returns:** No return value.

**Errors:** Raises `HostPrototypeError`.

---

#### execute_identity()

**Signature:**

```ruby
def self.execute_identity(provider_name, request)
```

**Example:**

```ruby
result = execute_identity("value", "data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `request` | `String` | Yes | The request |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

---

#### execute_async_identity()

**Signature:**

```ruby
def self.execute_async_identity(provider_name, request)
```

**Example:**

```ruby
result = execute_async_identity("value", "data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `request` | `String` | Yes | The request |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

---

#### start_identity_worker()

**Signature:**

```ruby
def self.start_identity_worker(provider_name, request)
```

**Example:**

```ruby
result = start_identity_worker("value", "data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `request` | `String` | Yes | The request |

**Returns:** `Integer`

**Errors:** Raises `HostPrototypeError`.

---

#### prepare_identity_worker()

**Signature:**

```ruby
def self.prepare_identity_worker(provider_name, request)
```

**Example:**

```ruby
result = prepare_identity_worker("value", "data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `request` | `String` | Yes | The request |

**Returns:** `Integer`

**Errors:** Raises `HostPrototypeError`.

---

#### dispatch_identity_worker()

**Signature:**

```ruby
def self.dispatch_identity_worker(task_id)
```

**Example:**

```ruby
dispatch_identity_worker(42)
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `task_id` | `Integer` | Yes | The task id |

**Returns:** No return value.

**Errors:** Raises `HostPrototypeError`.

---

#### cancel_identity_worker()

**Signature:**

```ruby
def self.cancel_identity_worker(task_id)
```

**Example:**

```ruby
cancel_identity_worker(42)
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `task_id` | `Integer` | Yes | The task id |

**Returns:** No return value.

**Errors:** Raises `HostPrototypeError`.

---

#### identity_worker_cancelled()

**Signature:**

```ruby
def self.identity_worker_cancelled(task_id)
```

**Example:**

```ruby
result = identity_worker_cancelled(42)
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `task_id` | `Integer` | Yes | The task id |

**Returns:** `Boolean`

**Errors:** Raises `HostPrototypeError`.

---

#### poll_identity_worker()

**Signature:**

```ruby
def self.poll_identity_worker(task_id)
```

**Example:**

```ruby
result = poll_identity_worker(42)
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `task_id` | `Integer` | Yes | The task id |

**Returns:** `String?`

**Errors:** Raises `HostPrototypeError`.

---

#### start_host_runtime()

**Signature:**

```ruby
def self.start_host_runtime()
```

**Example:**

```ruby
start_host_runtime()
```

**Returns:** No return value.

**Errors:** Raises `HostPrototypeError`.

---

#### shutdown_host_runtime()

**Signature:**

```ruby
def self.shutdown_host_runtime(timeout_millis, force)
```

**Example:**

```ruby
result = shutdown_host_runtime(42, true)
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `timeout_millis` | `Integer` | Yes | The timeout millis |
| `force` | `Boolean` | Yes | The force |

**Returns:** `Array<Integer>`

**Errors:** Raises `HostPrototypeError`.

---

#### execute_typed_identity()

**Signature:**

```ruby
def self.execute_typed_identity(provider_name, source_ids, source_lengths, source_digests, source)
```

**Example:**

```ruby
result = execute_typed_identity("value", [], [], [], "data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `source_ids` | `Array<String>` | Yes | The source ids |
| `source_lengths` | `Array<Integer>` | Yes | The source lengths |
| `source_digests` | `Array<String>` | Yes | The source digests |
| `source` | `String` | Yes | The source |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

---

#### execute_typed_workflow()

**Signature:**

```ruby
def self.execute_typed_workflow(provider_name, source_ids, source_lengths, source_digests, source)
```

**Example:**

```ruby
result = execute_typed_workflow("value", [], [], [], "data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `source_ids` | `Array<String>` | Yes | The source ids |
| `source_lengths` | `Array<Integer>` | Yes | The source lengths |
| `source_digests` | `Array<String>` | Yes | The source digests |
| `source` | `String` | Yes | The source |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

---

#### execute_in_process_identity()

**Signature:**

```ruby
def self.execute_in_process_identity(source_ids, source_lengths, source_digests, source)
```

**Example:**

```ruby
result = execute_in_process_identity([], [], [], "data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `source_ids` | `Array<String>` | Yes | The source ids |
| `source_lengths` | `Array<Integer>` | Yes | The source lengths |
| `source_digests` | `Array<String>` | Yes | The source digests |
| `source` | `String` | Yes | The source |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

---

#### execute_detached_identity()

**Signature:**

```ruby
def self.execute_detached_identity(provider_name, source_ids, source_lengths, source_digests, source)
```

**Example:**

```ruby
result = execute_detached_identity("value", [], [], [], "data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `source_ids` | `Array<String>` | Yes | The source ids |
| `source_lengths` | `Array<Integer>` | Yes | The source lengths |
| `source_digests` | `Array<String>` | Yes | The source digests |
| `source` | `String` | Yes | The source |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

---

#### registered_workflow_hosts()

**Signature:**

```ruby
def self.registered_workflow_hosts()
```

**Example:**

```ruby
result = registered_workflow_hosts()
```

**Returns:** `Array<String>`

---

#### probe_with_parser()

**Signature:**

```ruby
def self.probe_with_parser(provider_name, request)
```

**Example:**

```ruby
result = probe_with_parser("value", "data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `request` | `String` | Yes | The request |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

---

#### parse_with_parser()

**Signature:**

```ruby
def self.parse_with_parser(provider_name, request)
```

**Example:**

```ruby
result = parse_with_parser("value", "data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `provider_name` | `String` | Yes | The provider name |
| `request` | `String` | Yes | The request |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

---

#### registered_parser_hosts()

**Signature:**

```ruby
def self.registered_parser_hosts()
```

**Example:**

```ruby
result = registered_parser_hosts()
```

**Returns:** `Array<String>`

---

#### capability_manifest()

**Signature:**

```ruby
def self.capability_manifest()
```

**Example:**

```ruby
result = capability_manifest()
```

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

---

#### merge_json_two_way()

**Signature:**

```ruby
def self.merge_json_two_way(incoming_source, current_source, dialect)
```

**Example:**

```ruby
result = merge_json_two_way("value", "value", "value")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `incoming_source` | `String` | Yes | The incoming source |
| `current_source` | `String` | Yes | The current source |
| `dialect` | `String` | Yes | The dialect |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

---

#### merge_json_three_way()

**Signature:**

```ruby
def self.merge_json_three_way(base_source, ours_source, theirs_source, dialect)
```

**Example:**

```ruby
result = merge_json_three_way("value", "value", "value", "value")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `base_source` | `String` | Yes | The base source |
| `ours_source` | `String` | Yes | The ours source |
| `theirs_source` | `String` | Yes | The theirs source |
| `dialect` | `String` | Yes | The dialect |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

---

### Types

#### HostBatchRequest

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `schema` | `String` | — | Schema |
| `items` | `Array<HostSourceSegment>` | — | Items |

---

#### HostPrototypeError

##### Methods

###### new()

**Signature:**

```ruby
def self.new(message)
```

**Example:**

```ruby
result = HostPrototypeError.new("value")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `message` | `String` | Yes | The message |

**Returns:** `HostPrototypeError`

###### message()

**Signature:**

```ruby
def message()
```

**Example:**

```ruby
result = instance.message()
```

**Returns:** `String`

---

#### HostSourceSegment

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `source_id` | `String` | — | Source id |
| `offset` | `Integer` | — | Offset |
| `byte_length` | `Integer` | — | Byte length |
| `sha256` | `String` | — | Sha256 |

---

#### ParserHost

##### Methods

###### descriptor()

**Signature:**

```ruby
def descriptor()
```

**Example:**

```ruby
result = instance.descriptor()
```

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

###### probe_batch()

**Signature:**

```ruby
def probe_batch(request)
```

**Example:**

```ruby
result = instance.probe_batch("data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `request` | `String` | Yes | The request |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

###### parse_batch()

**Signature:**

```ruby
def parse_batch(request)
```

**Example:**

```ruby
result = instance.parse_batch("data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `request` | `String` | Yes | The request |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

---

#### Plugin

##### Methods

###### name()

**Signature:**

```ruby
def name()
```

**Example:**

```ruby
result = instance.name()
```

**Returns:** `String`

###### version()

**Signature:**

```ruby
def version()
```

**Example:**

```ruby
result = instance.version()
```

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

###### initialize()

**Signature:**

```ruby
def initialize()
```

**Example:**

```ruby
instance.initialize()
```

**Returns:** No return value.

**Errors:** Raises `HostPrototypeError`.

###### shutdown()

**Signature:**

```ruby
def shutdown()
```

**Example:**

```ruby
instance.shutdown()
```

**Returns:** No return value.

**Errors:** Raises `HostPrototypeError`.

---

#### WorkflowHost

##### Methods

###### descriptor()

**Signature:**

```ruby
def descriptor()
```

**Example:**

```ruby
result = instance.descriptor()
```

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

###### execute_batch()

**Signature:**

```ruby
def execute_batch(request)
```

**Example:**

```ruby
result = instance.execute_batch("data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `request` | `String` | Yes | The request |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

###### execute_cancellable_batch()

**Signature:**

```ruby
def execute_cancellable_batch(task_id, request)
```

**Example:**

```ruby
result = instance.execute_cancellable_batch(42, "data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `task_id` | `Integer` | Yes | The task id |
| `request` | `String` | Yes | The request |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

###### execute_async_batch()

**Signature:**

```ruby
def execute_async_batch(request)
```

**Example:**

```ruby
result = instance.execute_async_batch("data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `request` | `String` | Yes | The request |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

###### execute_typed_batch()

**Signature:**

```ruby
def execute_typed_batch(request, source)
```

**Example:**

```ruby
result = instance.execute_typed_batch(HostBatchRequest.new, "data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `request` | `HostBatchRequest` | Yes | The host batch request |
| `source` | `String` | Yes | The source |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

###### execute_detached_batch()

**Signature:**

```ruby
def execute_detached_batch(request)
```

**Example:**

```ruby
result = instance.execute_detached_batch("data")
```

**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `request` | `String` | Yes | The request |

**Returns:** `String`

**Errors:** Raises `HostPrototypeError`.

---

### Trait Bridges

| Trait | Register | Unregister | Clear |
|-------|----------|------------|-------|
| `WorkflowHost` | StructuredmergeHostPrototypeCore.register_workflow_host | StructuredmergeHostPrototypeCore.unregister_workflow_host | StructuredmergeHostPrototypeCore.clear_workflow_hosts |
| `ParserHost` | StructuredmergeHostPrototypeCore.register_parser_host | StructuredmergeHostPrototypeCore.unregister_parser_host | StructuredmergeHostPrototypeCore.clear_parser_hosts |
