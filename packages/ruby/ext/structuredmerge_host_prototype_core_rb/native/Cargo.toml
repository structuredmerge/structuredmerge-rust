# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:76362d6195fb0c78bbbd40e1f1b9ae2fa4a8894f35be5fb109d8d8c233b3f411
# To regenerate: alef generate
# To verify freshness: alef verify

[package]
name = "structuredmerge-host-prototype-core-rb"
version = "0.2.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Generated Ruby host-provider prototype for StructuredMerge"
readme = false
keywords = []
categories = []

[package.metadata.cargo-machete]
ignored = ["async-trait", "rb-sys", "tracing"]

[lib]
name = "structuredmerge_host_prototype_core_rb"
path = "../src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
async-trait = "0.1"
magnus = "0.8"
rb-sys = ">=0.9.130, <0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
structuredmerge-host-prototype-core = { version = "0.2.0", path = "../../../../../crates/structuredmerge-host-prototype-core" }
tokio = { version = "1", features = ["rt-multi-thread"] }
tracing = "0.1"

# This crate deliberately does not use `[lints]` / `workspace = true`: its C-ABI /
# PyO3 / napi / ext-php-rs / NIF boundary requires `unsafe` code, and the workspace's
# `[workspace.lints.rust]` sets `unsafe_code = "deny"` -- an all-or-nothing table that
# would turn every such boundary into a compile error. The `[lints.clippy]` block below
# instead carries the subset of the workspace's deny-by-default lint policy this crate
# can actually satisfy. ~keep
[lints.clippy]
dbg_macro = "deny"
print_stderr = "deny"
print_stdout = "deny"
