[package]
name = "toml-merge"
version = "0.0.0"
edition = "2024"

[dependencies]
ast-merge = { path = "../ast-merge" }
pest = "2.8"
pest_grammars = "2.8.6"
serde = { version = "1", features = ["derive"] }
toml = "0.9"
tree-haver = { path = "../tree-haver" }

[dev-dependencies]
serde_json = "1"
